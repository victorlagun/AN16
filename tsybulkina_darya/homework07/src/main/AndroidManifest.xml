<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techmeskills.an16.lagun_victor.homework04">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_sleep_tracker"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_sleep_tracker_round"
        android:supportsRtl="true"
        android:name="com.techmeskills.an16.homework04.App"
        android:theme="@style/AppTheme">
        <activity android:name="com.techmeskills.an16.homework04.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
